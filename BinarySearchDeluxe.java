package A03;

import java.util.Comparator;

public class BinarySearchDeluxe {

	/*
	 * Corner cases. Each static method should throw a
	 * java.lang.NullPointerException if any of its arguments is null. You
	 * should assume that the argument array is in sorted order (with respect to
	 * the supplied comparator).
	 * 
	 * Performance requirements. The firstIndexOf() and lastIndexOf() methods
	 * should make at most 1 + [log2 N] compares in the worst case,
	 */

	// Return the index of the first key in a[] that equals the search key, or
	// -1 if no such key.
	public static <Key> int firstIndexOf(Key[] a, Key key, Comparator<Key> comparator) {
		return 0; // TODO
	}

	// Return the index of the last key in a[] that equals the search key, or -1
	// if no such key.
	public static <Key> int lastIndexOf(Key[] a, Key key, Comparator<Key> comparator) {
		return 0; // TODO
	}
}